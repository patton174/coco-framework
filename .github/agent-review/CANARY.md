# Agent Review Jury Canary

This transient pull request verifies the protected-base review workflow without
changing framework or release behavior. It must exercise five specialist model
calls, two verifier model calls, one chair model call, deterministic publication,
and the managed `Agent jury gate` status.
