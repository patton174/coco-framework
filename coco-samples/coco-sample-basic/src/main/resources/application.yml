coco:
  features:
    exclude:
      - tenant
      - data-permission
  common:
    i18n:
      basename:
        - messages
        - coco-messages
      default-locale: zh-CN
      fallback-to-system-locale: false
      use-code-as-default-message: true
